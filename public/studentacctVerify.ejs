<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>identity verification</title>
    <style>
        body {
            margin-top: 10%;
            background-image: linear-gradient(90deg,
                    rgba(56, 166, 199, 0.6),
                    rgb(117, 142, 148));
        }

        .msg {
            margin-left: 40%;
            margin-bottom: 10px;
            text-align: center;
            width: max-content;
            height: 20px;
            border: 1px solid black;
            background-color: #4cbbbb;
            border-radius: 20px;
            padding: 40px;
        }

        .hidden {
            display: none;
        }

        .container {
            width: 40%;
            max-width: 80%;
            margin: auto;
            border-radius: 20px;
            background-color: #91c7b9;
            padding: 40px;
            height: 100%;
            opacity: 0.9;

            filter: drop-shadow(0 0 10px #555345e6);

        }

        .container h2 {
            color: rgb(82, 103, 219);
        }

        button {
            display: block;
            /* margin: 20px 12px 12px 0px; */
            width: max-content;
            height: 30px;
            background-color: #4cbbbb;
            border-radius: 20px;
            border: none;
            text-align: center;
            padding: 20px;
            font-size: 15px;

        }

        button:hover {
            cursor: pointer;
        }


        label {
            color: black;
            display: block;
            margin: auto;
            margin-top: 10px;

        }

        input {
            display: block;
            margin: 20px 12px 12px 0px;
            width: 100%;
            height: 30px;
            background-color: #f0f3f3;
            border-radius: 20px;
            border: none;
            padding: 10px;
        }

        @media (max-width: 900px) {

            .container {

                width: 80%;
            }



        }
    </style>
</head>

<body>
    <!-- <div class="msg hidden">
        Account Created successfully.
    </div> -->
    <div class="container">
        <h2>Create password to access our features..</h2>
        <form action="/studentacctVerify" method='post' id="form">
            <label for="studentId">Enter student Id:</label>
            <input type="text" id="studentId" name="studentId" required>
            <label for="createPasswd">Enter New Password:</label>
            <input type="password" id="createPasswd" name="createPasswd" required>
            <label for="confirmCreatePasswd">Confirm Password:</label>
            <input type="password" id="confirmCreatePasswd" name="confirmCreatePasswd" required>
            <button id="btn"> Create Password</button>
        </form>
    </div>
    <script>

        // const btn = document.querySelector("#btn")
        // const msg = document.querySelector(".msg")

        // btn.addEventListener("click", (e) => {
        //     e.preventDefault();
        //     msg.classList.remove("hidden")

        // })

        // const form = document.querySelector("#form")
        // const formData = new FormData(e.target);
        // form.addEventListener("submit"), async (e) => {
        //     e.preventDetault();

        //     await axios.post("/studentacctVerify", formData)

        // }

    </script>
</body>

</html>